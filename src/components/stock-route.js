// DEAD FILE — SAFE TO DELETE (`git rm src/components/stock-route.js`).
// This was a stale copy of src/app/api/connectors/stock/route.js sitting in the
// components folder. Nothing imports it and it is not a route: files under
// src/components are never served. Emptied rather than removed only because the
// tooling used to open this PR can write files but cannot delete them.
